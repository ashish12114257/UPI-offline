# API Configuration — set to your deployed backend URL
# Development: leave as full URL (no Vite proxy needed)
# Production: use the same backend URL
VITE_API_BASE_URL=https://upi-offline-mesh-3.onrender.com
